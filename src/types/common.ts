export interface ISettings {
  appID: string;
  secretKey: string;
}

export interface IAliExpress {
  appID: string;
  secretKey: string;
}

export interface ISallaAccount {
  clientID: string;
  clientSecretKey: string;
}
